# =============================================================================
# 消融实验 D: Full (三层级联 - Safety + STAPO + Learned)
# =============================================================================

model:
  scene_dim: 256
  plan_len: 6
  hidden_dim: 256
  dropout: 0.1
  log_std_min: -5.0
  log_std_max: 2.0
  action_scale: 1.0

# ✅ 启用所有三层防御
safety_guard:
  enabled: true
  max_residual_norm: 5.0
  max_step_disp: 2.0
  max_speed: 15.0
  max_total_disp: 10.0
  dt: 0.5

stapo_gate:
  enabled: true
  advantage_threshold: 0.0
  probability_threshold: 0.1
  entropy_threshold: 0.5
  entropy_normalization: max
  min_retention_ratio: 0.1
  use_combined_threshold: true

# ✅ 启用 LearnedUpdateGate
learned_gate:
  enabled: true
  evaluator_path: /mnt/cpfs/prediction/lipeinan/RL/E2E_RL/experiments/update_evaluator_v1/best_evaluator.pth
  tau_gain: 0.0
  tau_risk: 0.5

data:
  data_dir: /mnt/cpfs/prediction/lipeinan/RL/E2E_RL/data/vad_dumps
  batch_size: 8
  num_workers: 0

training:
  bc:
    enabled: true
    epochs: 5
    lr: 1.0e-4
    weight_decay: 1.0e-4
    grad_clip: 1.0

  rl:
    enabled: true
    epochs: 20
    lr: 5.0e-5
    weight_decay: 1.0e-4
    grad_clip: 1.0
    entropy_coef: 0.01

  scheduler:
    type: cosine
    T_max: 30
    eta_min: 1.0e-06

  reward:
    dt: 0.5
    w_progress: 1.0
    w_collision: 0.5
    w_offroad: 0.3
    w_comfort: 0.1

output_dir: /mnt/cpfs/prediction/lipeinan/RL/E2E_RL/experiments/ablation_full
save_every: 5
log_interval: 10
